// Variable declarations
const websiteName = "My Portfolio"; // Cannot be reassigned
let visitorCount = 42;             // Can be reassigned

// Basic arithmetic operation
visitorCount = visitorCount + 1;

// Outputting text using template literals
console.log(`Welcome to ${websiteName}! Total visitors: ${visitorCount}`);
